DATABASE_URL="file:./dev.db"

# Staff desk lock. Each person verifies their @nzcenergy.co.uk work email,
# then creates their own password. Do not put a password or API key in git.
# Joel adds these himself — do not paste them into chat.
# Do not invent other people’s addresses. Do not expose this CRM on a public URL.
# Optional cookie-signing key (not a login password).
CRM_SESSION_SECRET=
# Required to send the first-time verification email. Missing = fail closed.
RESEND_API_KEY=
# From-address for that email. Defaults to info@nzcenergy.co.uk.
CRM_MAIL_FROM=
# Public origin used to build /login/verify?token=… (or the request host).
# Example locally: http://localhost:3000
CRM_PUBLIC_URL=

# Optional DocuSign. Leave blank to preview/download the filled LOA only.
DOCUSIGN_TEMPLATE_ID=
DOCUSIGN_ACCOUNT_ID=
DOCUSIGN_ACCESS_TOKEN=
DOCUSIGN_INTEGRATION_KEY=
DOCUSIGN_USER_ID=
DOCUSIGN_PRIVATE_KEY=
DOCUSIGN_BASE_PATH=https://demo.docusign.net/restapi
DOCUSIGN_AUTH_SERVER=account-d.docusign.com
DOCUSIGN_SIGNER_ROLE=Customer
DOCUSIGN_RETURN_URL=
# Required to accept Connect webhooks. Missing secret = reject (fail closed).
DOCUSIGN_WEBHOOK_SECRET=

# Optional local-only customer portal demo login.
# Rotate any old demo passwords/tokens that were previously in source.
# Never commit real values. Do not put customer names in these replacements.
PORTAL_DEMO_EMAIL=
PORTAL_DEMO_PASSWORD=
PORTAL_DEMO_TOKEN=
